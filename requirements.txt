Flask==2.2.2
flask-cors==3.0.10
gunicorn==20.1.0
numpy
pandas
scikit-learn  # Ensure this matches the version used for model serialization, if you've followed previous advice
Werkzeug>=2.2.2  # Updated to meet Flask's requirements
